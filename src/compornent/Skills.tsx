import React from 'react'

type SkillsProps = {
  skills: {
    heading: string;
  }
}

const Skills = ({skills}: SkillsProps) => {
  return (
      <div className={ }>
      <h2>{skills.heading}</h2>
      <div className={ }>
        
          </div>
      </div>
  )
}

export default Skills